apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: aks-journal-app
type: Opaque
stringData:
  # These values will be replaced by azd during deployment
  REDIS_HOST: "${AZURE_REDIS_HOST}"
  REDIS_PORT: "${AZURE_REDIS_PORT}"
  REDIS_PASSWORD: "${AZURE_REDIS_PASSWORD}"
